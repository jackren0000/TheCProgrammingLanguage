//The unary operators * and & bind more tightly than arithmetic operators.
//Thus, *p+1 means 'take the object that p points to, and add 1 to it.auto

#include <stdio.h>

int main() {
    printf("The value of EOF is %d", EOF);
}